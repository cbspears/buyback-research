# Optional API keys — free tiers work for all collectors
# CoinGecko demo key (free, higher rate limits)
COINGECKO_API_KEY=
# Etherscan (for on-chain queries)
ETHERSCAN_API_KEY=
# Infura/Alchemy RPC (fallback — public RPCs used by default)
ETH_RPC_URL=
# Anthropic (used by loop.sh via claude CLI)
ANTHROPIC_API_KEY=
